import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.loomstudio.merge2048',
  appName: 'Merge 2048',
  webDir: 'dist',
  android: {
    backgroundColor: '#faf8ef',
  },
};

export default config;
